import { MotionBar, StaticBar } from "../../../styles/styled/div";

const animatedBar = () => {
   return (
      <StaticBar>
         <MotionBar />
      </StaticBar>
   );
};

export default animatedBar;
